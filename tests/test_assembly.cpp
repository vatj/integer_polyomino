#include <catch.hpp>

// #include "integer_model.hpp"

// PhenotypeTable pt = PhenotypeTable();
// // PhenotypeTable::phenotype_builds = 50;
// // PhenotypeTable::UND_threshold = 0.25;
// // pt.FIXED_TABLE = false;
//
// Genotype genome_dimer{0,0,0,1,0,0,0,2};
// Genotype genome_tetramer{0,0,1,2};
//
// std::vector <Phenotype_ID> dimer{std::make_pair(2,0)};
// // tetramer = {4,0};
// // Phenotype_ID rare = {0,0}, unbound = {255,0};
//
// TEST_CASE("Dimer Assembly")
// {
// 	REQUIRE(AssemblePlasticGenotype(genome_dimer, &pt) == dimer);
// 	// REQUIRE(AssemblePlasticGenotype(genome_tetramer, &pt) == std::vector <Phenotype_ID> (tetramer));
// }
